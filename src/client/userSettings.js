const userSettings = {
  // Insulin action duration (in minutes)
  insulinActionDuration: 210,

  // Time (in minutes) to peak insulin effect TODO: at some point may want secondary peak setting for large bolus amounts which can extend duration and peak times, more research required
  peak: 90,
};

export default userSettings;
